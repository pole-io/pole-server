---
description: apis 与 plugin 分层及接口实现规范
globs: "apis/**/*.go", "plugin/**/*.go", "pkg/**/*.go"
alwaysApply: false
---

# apis 与 plugin 分层

## 分层约定

- **apis/**：定义对外契约与存储/能力接口（如 `apis/store`、`apis/access_control/auth`），不包含具体实现。
- **plugin/**：接口的多种实现（如 `plugin/store/mysql`、`plugin/access_control/auth`）。
- **pkg/**：可复用的业务逻辑与领域服务（如 `pkg/goverrule`、`pkg/service`），依赖 apis 接口，不依赖 plugin 实现。

## 接口定义

- 接口在 apis 中定义，方法注释用中文说明用途（如 `// CreateRateLimit 新增限流规则`）。
- 组合小接口形成大接口（如 `GovernanceStore` 组合 `RateLimitStore`、`CircuitBreakerStore` 等）。

## 实现侧

- 实现处用编译期断言保证实现接口：`var _ store.CircuitBreakerStore = (*circuitBreakerStore)(nil)`。
- MySQL 存储实现放在 `plugin/store/mysql`，包名为 `sqldb`；通用规则逻辑可抽到 `BaseRuleStore` + `RuleOperation`，事务与重试由 `ExecuteRuleOperation`/`ExecuteRuleOperationTx` 统一处理。
