---
description: Go 日志规范
globs: "**/*.go"
alwaysApply: false
---

# 日志规范

## 包与用法

- 使用项目全局 `log`（如 `go.uber.org/zap` 封装）及 `go.uber.org/zap` 的 `zap.Error()`、`zap.String()` 等。

## 请求上下文中必须带 RequestID

在带有 `context.Context` 的 API/业务逻辑中，打日志时附带请求 ID，便于排查：

```go
// ✅ 正确
log.Error(err.Error(), utils.RequestID(ctx))
log.Info(msg, utils.RequestID(ctx))
log.Error("get circuitbreaker rules from cache", utils.RequestID(ctx), zap.Error(err))
```

```go
// ❌ 错误：有 ctx 却未带 RequestID
log.Error(err.Error())
```

## 存储/批量逻辑

- 无 ctx 的存储层可用带语义前缀的格式：`log.Errorf("[Store][Lane] ...", ...)` 或 `log.Error("[store][mysql][ratelimit] ...", zap.Error(err))`。
- 批量进度类可用 `log.Infof` 按进度打点（如每 200/100 条）。
