---
description: Go 文件头、package 规范及公开方法首参必须为 context.Context
globs: "**/*.go"
alwaysApply: false
---

# Go 文件头与包规范

## 文件头

所有 Go 源文件使用统一的 BSD 3-Clause 版权头（Tencent/Polaris）：

```go
/**
 * Tencent is pleased to support the open source community by making Polaris available.
 *
 * Copyright (C) 2019 THL A29 Limited, a Tencent company. All rights reserved.
 *
 * Licensed under the BSD 3-Clause License (the "License");
 * ...
 * https://opensource.org/licenses/BSD-3-Clause
 * ...
 */
```

- 版权块后紧跟 `package` 声明，再是 `import`。
- 不要省略或改写版权头。

## Package 与 import

- **包名**：小写、简短，与目录名一致（如 `sqldb`、`policy`、`store`）。
- **导入分组**：标准库 → 空行 → 第三方/项目内包；同一组内按字母序。
- **别名**：长包名使用语义化别名，如 `apimodel`、`storeapi`、`cachetypes`、`authtypes`。

## 公开方法签名

- **导出（public）方法**：第一个参数必须为 `context.Context`，用于传递请求 ID、超时与取消。
- 接口定义与实现均需遵守；仅包内未导出的辅助函数可省略 ctx。

```go
// ✅ 正确
func (s *Server) CreateCircuitBreakerRules(ctx context.Context, request []*apifault.CircuitBreakerRule) *apimodel.BatchWriteResponse
func (c *circuitBreakerStore) GetMoreCircuitBreakers(ctx context.Context, mtime time.Time, firstUpdate bool) ([]*rules.CircuitBreakerRule, error)

// ❌ 错误：导出方法首参不是 context.Context
func (s *Server) CreateRule(request *Request) *Response
```

## 注释

- 对外接口（interface、exported 类型/函数）必须写注释；业务逻辑注释可用中文。
- 格式：`// MethodName 简短说明` 或 `// 业务说明`。
