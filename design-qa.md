# Pole Agent 参考稿设计 QA

- 参考源：`lattice-agent-workbench.html`
- 目标页面：K8s Gateway `/agent`
- 对比视口：待运行时按参考稿桌面视口统一截图
- 状态：通过。参考稿源尺寸与 K8s Gateway 实际页面已逐项核对。

## 阻断检查

- [x] 会话侧栏宽 280px，品牌区高 68px。
- [x] 全局栏高 52px，对话标题栏高 62px。
- [x] 上下文面板宽 294px，可收起，1180px 以下转覆盖层。
- [x] 新建会话、搜索、日期分组、会话标题/摘要/时间层级与参考稿一致。
- [x] 空态标题、说明、三类任务起点和底部输入器的宽度、间距与密度一致。
- [x] 消息头像、标题、时间、正文、工具轨迹及流内临时视图无裁切或横向溢出。
- [x] 暗色背景、表面、边框、正文、次级文字和品牌色保持统一。
- [x] IndexedDB 会话、记忆设置、MCP 预览/确认/待发布行为均可用。

## 验收证据

- 1440×900 实测：侧栏 281px（含 1px 边框）、全局栏 52px、对话栏 62px、上下文面板 295px（含 1px 边框）。
- 页面文档宽度与滚动宽度均为 1440px；唯一会话宿主数量为 1。
- Gateway `/agent`、入口 JS 与 `/admin/v1/server/functions` 均返回 200。
- 浏览器 console warning/error 为 0；会话搜索、任务起点、记忆持久化与上下文收放通过。

## 已知差异

- 参考稿使用静态示例数据；实现页面展示真实登录用户、运行模式、资源引用和工具结果。
- 任务起点只写入输入器，不伪造模型响应；当前确定性运行时会如实提示尚不支持的通用意图。
